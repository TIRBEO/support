type EmailTemplate = {
    subject: string;
    html: string;
};
declare function otpCodeBlock(code: string): string;
declare function buttonBlock(url: string, label: string): string;
declare const EMAIL_TEMPLATES: Record<string, (logo: string, imageBase: string) => EmailTemplate>;
declare function buildTemplates(logoUrl?: string, imageBase?: string): Record<string, EmailTemplate>;
declare function renderTemplate(html: string, vars: Record<string, string>): string;

export { EMAIL_TEMPLATES, type EmailTemplate, buildTemplates, buttonBlock, otpCodeBlock, renderTemplate };
